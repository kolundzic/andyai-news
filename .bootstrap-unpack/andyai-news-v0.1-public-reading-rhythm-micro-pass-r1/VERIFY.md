# VERIFY — Public Reading Rhythm Micro Pass

Confirm the following after apply:

1. `app/archive/[locale]/[day]/page.tsx` is updated.
2. `app/[locale]/edition/[day]/page.tsx` is updated.
3. `app/news/[id]/page.tsx` is updated.
4. Each reading surface now has a clearer "Reading rhythm" layer.
5. Release notes file exists in repo root.
6. Commit, tag, and GitHub release complete successfully.

This pack should not change routes, API behavior, or admin pages.
