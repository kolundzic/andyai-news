export const HISTORY_SNAPSHOT_PATH = "data/history/history-snapshot.json";

export const HISTORY_DEFAULT_LOCALES = ["en"];

export const HISTORY_FEATURES = {
  archiveMode: true,
  localeAwareHistory: false,
  publicArchiveRoutes: false,
} as const;
